<%= form_for(@type_form_field) do |f| %>
  <% if @type_form_field.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_form_field.errors.count, "error") %> prohibited this type_form_field from being saved:</h2>

      <ul>
      <% @type_form_field.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
